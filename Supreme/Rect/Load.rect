rect(version == 1.5.4);
  class(
    name = "StorageManager",
    do(
      
    )
  )
  class(
    name = "EnvironmentManager",
    do(
      add.parser("env")(env_usage);
    )
  )
{
    attempt(
        rect(version == TRUE);
    )
}